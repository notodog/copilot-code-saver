[package]
name = "ccs-host"
version = "0.2.0"
edition = "2021"
description = "Native messaging host for Copilot Code Saver (stateless file writer)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = "z"
lto = true
strip = true
